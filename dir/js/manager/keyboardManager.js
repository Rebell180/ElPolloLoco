
/**
 * 
 */
export class KeyboardManager {

    static KEY_A = false;      // Go Left
    static KEY_D = false;      // Go Right
    static KEY_SPACE = false;  // Jump
    static KEY_F = false;      // Throw


    static KeyDown(value) {
        switch(value) {

            default: break;
        }
    }

    static KeyUp(value) {
        switch(value) {
            
            default: break;
        }
    }
}   